Four developer-facing pages are capping their content at a fixed max-width that's much narrower than the actual browser viewport, leaving large dead margins on both sides and empty vertical space below the content. Widen the container on each, and standardize them to the same width so the developer portal feels consistent:

client/src/pages/developer-crm.tsx, line 362: change <main className="mx-auto max-w-[1480px] px-4 py-6 sm:px-6 lg:px-10 lg:py-9"> to <main className="mx-auto max-w-[1680px] px-4 py-6 sm:px-6 lg:px-10 lg:py-9">.
client/src/pages/developer-outreach.tsx, line 499: change <main className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"> to <main className="mx-auto max-w-[1680px] px-4 py-8 sm:px-6 lg:px-8">.
client/src/pages/developer-analytics.tsx, line 160: change <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"> to <div className="mx-auto max-w-[1680px] px-4 py-8 sm:px-6 lg:px-8">.
client/src/pages/developer-criteria-settings.tsx, line 484: change <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8"> to <main className="mx-auto max-w-[1680px] px-4 py-8 sm:px-6 lg:px-8">. This page is currently the narrowest (max-w-5xl, ~1024px) which is why it looks the most cramped relative to its own sidebar and header width.
Also in developer-criteria-settings.tsx: the "Broker notifications" card (starts around line 497, closes around line 586) and the "Outreach sending mode" card (starts around line 588) are currently stacked full-width one above the other inside a <div className="space-y-6"> wrapper, even though they're both short, parallel settings sections — this wastes a lot of horizontal space now that the container is wider. Wrap just those two Cards in their own <div className="grid gap-6 lg:grid-cols-2">...</div> so they sit side by side on larger screens (matching the side-by-side "Sending account" / "Target audience" pattern already used in developer-outreach.tsx), while leaving the "Criteria" and "Team" cards below them full-width and untouched.

Don't change any padding/spacing inside the cards themselves, any form logic, or table/grid layouts elsewhere — this is purely about the outer container width and the one card-pairing change in #5.