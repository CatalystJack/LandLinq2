There are now two different page-header components doing the same job with different colors, which is exactly the inconsistency we're trying to eliminate. client/src/components/ui/page-header.tsx (PageHeader) is the correct one — it uses the real theme tokens (text-catalyst-navy, text-catalyst-blue, border-catalyst-gray-200) and is already used correctly on every admin page and on developer-criteria-settings.tsx. But client/src/components/developer-page-header.tsx (DeveloperPageHeader) is a separate component that got created and used instead on developer-analytics.tsx, developer-crm.tsx, developer-dashboard.tsx, developer-outreach.tsx, developer-page.tsx, developer-pipeline.tsx, and developer-user-management.tsx. It uses hardcoded hex colors that don't match our real brand tokens (#4A90E2 instead of the actual --catalyst-blue value #498EDE, #07172A instead of the actual --catalyst-navy value #081729) and a bolder heading weight (font-bold instead of font-semibold) than the real PageHeader.

Fix this by removing developer-page-header.tsx and switching every page that imports it over to the real PageHeader from @/components/ui/page-header:

1. In each of client/src/pages/developer-analytics.tsx, developer-crm.tsx, developer-dashboard.tsx, developer-outreach.tsx, developer-page.tsx, developer-pipeline.tsx, and developer-user-management.tsx: change the import from `import DeveloperPageHeader, { developerHeaderButtonClass } from "@/components/developer-page-header";` to `import { PageHeader } from "@/components/ui/page-header";`, and change every `<DeveloperPageHeader ... />` usage to `<PageHeader ... />` with the same props (title, description, eyebrow, actions) — PageHeader's props are a superset/match of DeveloperPageHeader's so this should be a direct swap.

2. Wherever developerHeaderButtonClass was used on action buttons inside those actions props, replace it with the Button component's existing size/variant props instead (e.g. size="sm" variant="outline") so those buttons also go through the shared Button styling rather than a separate hardcoded className string.

3. Delete client/src/components/developer-page-header.tsx once nothing imports it anymore.

4. client/src/pages/analyst-dashboard.tsx has no PageHeader at all — it still has its own hand-rolled title block. Find its top-level <h1>/description/action-button block and replace it with <PageHeader title="..." description="..." actions={...} /> the same way the other pages were converted, keeping the existing title/description text and moving any header action buttons into the actions prop.

After this, every page in the app should be using the single shared PageHeader component with the same colors. Don't change any business logic — this is a component-consolidation and visual pass only.