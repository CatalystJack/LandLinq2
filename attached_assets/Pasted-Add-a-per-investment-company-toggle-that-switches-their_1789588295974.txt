Add a per-investment-company toggle that switches their outreach sending between Live and Test mode, plus a button to fire one real test email, in the company's own Settings tab.

1. In shared/schema.ts, add a new column to the developerProfiles table (around line 34-43, alongside isInternal): outreachTestModeEnabled: boolean("outreach_test_mode_enabled").default(false). Run npm run db:push (drizzle-kit push) to apply it.

2. In server/services/outreachService.ts, wherever a campaign is about to send real messages (the sendMessage method, around line 316-390, and wherever it's invoked with a dryRun option), look up the campaign's owning developer profile's outreachTestModeEnabled. If true, force this send to behave as a dry run (reuse the existing dryRun code path already in sendMessage — the one that logs "[DRY RUN] Would send..." and marks the message sent with metadata: { dryRun: true }) even if dryRun wasn't explicitly passed in. This protects a company's real brokers from receiving live outreach while that company is in test mode.

3. Add a new route in server/routes.ts: PATCH /api/developer-profile/me/outreach/test-mode, requiring isAuthenticated, that takes { enabled: boolean } in the body and updates the current session's developer profile's outreachTestModeEnabled column, returning the updated value. Only the profile's own team members should be able to change this — scope by the same developerProfileId pattern used by the other /api/developer-profile/me/outreach/* routes nearby (e.g. the one at line 15150 handling the sender endpoints).

4. In client/src/pages/developer-criteria-settings.tsx, add a new Card right after the "Broker notifications" card (which ends around line 540, inside the same space-y-6 container) titled "Outreach sending mode". It should show a Switch (from @/components/ui/switch) labeled "Test mode" with helper text: "When on, scheduled outreach campaigns won't send to real brokers — every send is logged as a dry run instead." Bind it to a useQuery for the current profile's outreachTestModeEnabled (add this field to whatever query already loads the profile, or add a small dedicated query if none exists) and a useMutation calling the new PATCH endpoint on toggle, with an optimistic or refetch-on-success update and a toast confirming the change.

5. In that same card, add a "Send live test email" button and a text input for a test recipient address. On click, POST to the existing /api/send-test-email endpoint (now fixed to really send, per the other prompt) using this company's current outreach email template and sender, and show a toast with the real success/failure result — this lets an admin verify actual deliverability for this company without touching any real broker.

Keep this scoped to developer-criteria-settings.tsx and the files listed above. Don't change the Broker notifications card that's already there.