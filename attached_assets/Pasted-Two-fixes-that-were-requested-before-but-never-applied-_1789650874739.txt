Two fixes that were requested before but never applied — please make the actual file changes this time and confirm what changed.

1. In server/routes.ts, the /api/test-email endpoint (around line 23162-23165) still has the comment "// For now, just return success as this is a test endpoint" with the real send commented out ("// await emailService.sendEmail(...)") and just returns { success: true, message: 'Test email sent successfully' } without sending anything. Fix it to actually send using sendNotificationEmail from server/emailService.ts — the same function /api/send-test-email (a bit further down in this file) already uses correctly for a real send. Call sendNotificationEmail({ to: email, subject: "LandLinq - Branding Settings Test Email", html: testEmailContent, text: "Branding settings test email", type: "test", priority: "high" }) using the testEmailContent already built earlier in this handler, and only return { success: true } if that call returns true — otherwise return a 500 with an error message.

2. In server/routes.ts, the /api/admin/analytics/by-profile endpoint (around line 12846-12930) and the engagement stats query further down (around line 16391) both compute "sent"/"opens"/"clicks"/"replies" from a LEFT JOIN LATERAL against outreach_message_events. That table is never written to anywhere in the codebase, so these numbers are always 0 regardless of real activity. Fix both queries to compute "sent" from the real, populated outreach_messages table instead: COUNT(*) FILTER (WHERE m.status = 'sent') AS sent, joining outreach_messages m to outreach_campaigns c (m.campaign_id = c.id) and filtering c.developer_profile_id = p.id AND COALESCE(c.is_archived, false) = false. Remove the opens/clicks/replies columns from both queries' results and from the SystemWideAnalytics summary object in client/src/pages/executive-dashboard.tsx, since we don't have real tracking data for those — don't display numbers as if we do. Keep "Outreach Sent" showing the real sent count in both places.

Confirm which lines in server/routes.ts actually changed for each of these two fixes.