Goal: Build a home/landing page for Investment Company (Developer-role) users, matching the existing Catalyst launchpad pattern (client/src/pages/launchpad.tsx) — their name, live market rates, a quick pipeline overview, and a section of their own saved tool/software links — and make it what they land on right after login instead of the deal dashboard.

Schema — new table developerQuickLinks in shared/schema.ts: id, developerProfileId (FK → developerProfiles, cascade), label (varchar, required), url (varchar, required), sortOrder (integer, default 0), isActive (boolean, default true), createdAt/updatedAt. No icon field — render every link with a generic link icon.
Server endpoints in server/routes.ts, following the existing getDeveloperProfileId(req, res) / requireActiveDeveloperProfile(...) pattern used elsewhere for developer-scoped data:
GET /api/developer/quick-links — current developer's active links, sorted by sortOrder.
POST /api/developer/quick-links — create, scoped to the requester's own profile.
PATCH /api/developer/quick-links/:id and DELETE /api/developer/quick-links/:id — only if the link's developerProfileId matches the requester's own profile (404 otherwise, don't leak whether a link exists).
New page client/src/pages/developer-home.tsx — same Market Rates and Pipeline Overview sections as launchpad.tsx, reusing /api/market-metrics and /api/deals/stats completely unchanged. Greet using the logged-in user's actual firstName (already on users via useAuth()), with developerProfile.companyName as subtitle. Add a "Your Tools" section below it, fetching /api/developer/quick-links and rendering each as a clickable card (label + external-link icon, opens in a new tab). Empty state links to Settings.
Settings UI — add a "Quick Links" section to client/src/pages/developer-criteria-settings.tsx, same card styling as the rest of the page: current links with edit/delete, plus an "Add Link" row (Label, URL, Save). No drag-and-drop reordering needed.
Routing — in client/src/App.tsx, add a route /developer/home → the new DeveloperHome component. In server/routes.ts, find getDeveloperHomePath (~line 2887) — it currently resolves to /developer/crm for general_sales profiles and /developer/dashboard for everyone else; change the non-general_sales branch to return /developer/home instead of /developer/dashboard. Leave the general_sales branch untouched. Add a "Home" tab as the first entry in realEstateTabs in client/src/components/developer-navigation.tsx, pointing to /developer/home. Keep /dashboard and /developer/dashboard working exactly as today for anyone who navigates there directly.

Don't modify /api/market-metrics, /api/deals/stats, AnalystDashboard, or the general_sales branch. Confirm when done that client/src/pages/developer-home.tsx actually exists as a file and that developerQuickLinks actually appears in shared/schema.ts — this exact prompt was sent once before and nothing got built, so please make sure the files are actually created this time, not just acknowledged.