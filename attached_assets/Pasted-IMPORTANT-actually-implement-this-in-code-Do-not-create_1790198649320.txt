IMPORTANT: actually implement this in code. Do not create a documentation file, notes file, or summary — make the actual code changes described below.

server/hellodataService.ts has a function getFilterCriteria(productType) (~line 100) that decides what counts as a "qualifying" nearby comparable property for deal classification — a minimum vintage year, minimum unit count, and minimum rent. Right now these numbers are hardcoded platform-wide by product type (2020+ vintage, 150+ units, $1.75/SF for conventional apartments and active adult; 2015+ vintage, 25+ units, $2,000 gross rent for BTR/Lot/Townhome/Single Family) — every investment company gets the same thresholds. There's no way for an individual company to set their own vintage-year and unit-count minimums, even though the search radius for this same comp search (compSearchRadiusMiles) is already a per-company setting.

Add two new per-company fields, following the exact same pattern as the existing compSearchRadiusMiles field:
- shared/schema.ts: add compMinVintageYear (integer, nullable, no default — null means "use the platform default for this product type") and compMinUnits (integer, nullable, no default) to the developerProfiles table, right next to compSearchRadiusMiles (~line 61).
- server/index.ts: add the matching ALTER TABLE ... ADD COLUMN IF NOT EXISTS migration for both new columns, in the same startup migration block where other developer_profiles columns are added.
- server/routes.ts: extend the save/validation logic that currently handles compSearchRadiusMiles (~line 15187-15199, the decimalFields block) to also accept compMinVintageYear and compMinUnits — these should be optional non-negative integers (compMinVintageYear should be a plausible 4-digit year, e.g. between 1900 and 2100, if provided; leaving either blank should store null, not zero).
- client/src/pages/developer-criteria-settings.tsx: add two new number inputs right next to the existing "Comp search radius" field (~line 1005-1015) — "Minimum comp vintage year" and "Minimum comp units" — both optional, with placeholder text explaining that leaving them blank uses the platform default for that product type.
- server/hellodataService.ts: change getFilterCriteria to accept optional companyMinVintage/companyMinUnits parameters and use them in place of the hardcoded minVintage/minUnits when provided, keeping the existing hardcoded values as the fallback when a company hasn't set their own (keep minGrossRent/minRentPSF exactly as they are today — only vintage and units become overridable for now).
- Update every call site of searchQualifyingComparables to pass the calling company's compMinVintageYear/compMinUnits through as companyMinVintage/companyMinUnits: server/automatedDealEmailPipeline.ts (~line 399, it already has the developer profile loaded there), server/autoClassificationEngine.ts (~line 388), and both call sites in server/routes.ts (~line 9428 and ~line 21300) — each of these will need to fetch or already have the relevant developer profile's compMinVintageYear/compMinUnits available before calling searchQualifyingComparables.

Verify it builds and confirm via git log that the commit contains a real diff to these files, not a text file.