IMPORTANT: actually implement this in code. Do not create a documentation file, notes file, or summary file.

There are multiple versions of the tagged NC and TN broker workbooks sitting in attached_assets/ (state + county + sector tagged from NCREC/TREC license lists). Use the most recently uploaded version of each — the filenames end in a timestamp, so pick the highest one for each state (as of now that looks like NC_Active_Brokers_Tagged_Sep2026_(2)_1790172814712.xlsx and TN_Active_Brokers_Tagged_Sep2026_(1)_1790172810576.xlsx, but confirm by checking actual file timestamps/sizes in attached_assets/ first in case a newer one has been added since).

Run scripts/import-broker-workbook.ts against the NC file, then again against the TN file, passing each path explicitly as the CLI argument (e.g. npx tsx scripts/import-broker-workbook.ts attached_assets/<NC file> and the same for TN — the script takes the workbook path as process.argv[2]). Report back the JSON output of each run (workbookRows, updatedByLicense, updatedByEmail, inserted) so I know exactly how many shared broker records were touched.
This import runs against the development database by default. Per this repo's own .agents/memory/broker-import-production-promotion.md note, production needs an explicit promotion step during publishing to pick up newly imported development data — follow that documented procedure to get this data into production (use the production-database-initialization option in the Publishing flow), don't just leave it in development.
After promoting to production, verify against the live production GET /api/crm/source-tags endpoint (or whichever endpoint currently backs the "Shared broker contacts" visibility-filter dropdowns in Developer Criteria Settings) that it now returns real state, county, and sector options instead of the empty "No contact state data is available yet" result. Report what that endpoint returns after the promotion.
Don't touch any per-company private CRM data, tags, notes, or assignments — this import only writes shared/global broker identity fields (state, county, sector, name, contact info) on records with no owning developer profile, exactly as the script already does. Don't modify the script's matching/dedupe logic.

Report back with the row counts from step 1 and the endpoint response from step 3 so I can confirm this actually fixed the empty-filters issue developers were seeing.

Sent. This one's more consequential than the others since it writes real data — Replit will report back the exact row counts and the post-promotion endpoint check, so we'll have concrete confirmation it actually fixed the empty broker-filter issue rather than just trusting a commit message.