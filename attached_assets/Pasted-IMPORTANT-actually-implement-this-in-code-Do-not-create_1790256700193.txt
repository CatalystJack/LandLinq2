IMPORTANT: actually implement this in code. Do not create a documentation file, notes file, or summary file — make the actual code changes described below.

1. Remove the "Broker Approvals" tab. In client/src/pages/user-management.tsx, delete the <TabsTrigger value="approvals">Broker Approvals</TabsTrigger> and its matching <TabsContent value="approvals">...</TabsContent> block (the "Partner Broker Approvals" card with the pending-brokers list and Approve buttons). Since only one tab will remain, remove the Tabs/TabsList/TabsTrigger wrapper entirely and just render the Users content directly — change the TabsList grid from 2 columns down accordingly, or drop the tabs UI altogether since there's nothing left to switch between. Also remove the now-unused pendingBrokers query and approveBrokerMutation, and clean up any now-unused imports (Clock, CheckCircle2, UserCheck, Loader2, Mail icons, etc.) so the build doesn't warn about unused code — but only remove an import if it's not used anywhere else in the file.

2. Show the actual company name instead of "Investment Company Team". Right now every Investment Company user gets the same generic badge. Change it so the badge shows their real company name:

In server/routes.ts, in the GET /api/users handler (~line 3260), the code already enriches BROKER users with broker-specific fields. Add the same kind of enrichment for DEVELOPER users: for each user with role === 'DEVELOPER' and a developerProfileId, look up that developer profile's companyName (query the developerProfiles table by id) and attach it to the returned user object as companyName. Batch this efficiently (e.g. fetch all needed developer profiles in one query by the set of developerProfileIds) rather than one query per user.
In client/src/pages/user-management.tsx, add companyName?: string to the User interface, and update getRoleLabel so that for a DEVELOPER user it returns user.companyName when present, falling back to "Investment Company Team" only if the company name is missing for some reason.

Leave the "All Roles" filter dropdown option and the Edit-user dialog copy alone — those are generic by design and don't need to change.