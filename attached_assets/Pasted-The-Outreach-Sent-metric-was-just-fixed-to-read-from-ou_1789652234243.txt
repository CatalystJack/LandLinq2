The "Outreach Sent" metric was just fixed to read from outreach_messages, but that only counts one of two outreach send paths. There's a second, separate mechanism — tag-triggered drip campaigns — tracked in drip_campaign_enrollments (schema around line 3002 in shared/schema.ts), which has totalStepsSent (integer, how many steps have actually been sent for that enrollment) and senderId (a foreign key to outreach_senders, which has developer_profile_id — so it's attributable to a company the same way outreach_messages already is). The comment already in the codebase at server/routes.ts around line 30284 confirms: "actual sends are tracked by updating last_sent_at + total_steps_sent in drip_campaign_enrollments, NOT in outreach_messages. Microsoft Graph drip worker never inserts into outreach_messages." This is a real, actively-used send path (the CRM tag-triggered drip campaign feature), so the current "sent" count is still an undercount for any company using it.

Fix both places that compute "Outreach Sent":

1. In server/routes.ts, the /api/admin/analytics/by-profile endpoint's outreach_stats LEFT JOIN LATERAL (around line 12916-12923, the one that was just fixed to use outreach_messages) needs to add drip campaign sends to the same "sent" total for each profile. Add a second LEFT JOIN LATERAL against drip_campaign_enrollments joined through outreach_senders (dce.sender_id = sender.id AND sender.developer_profile_id = p.id), summing dce.total_steps_sent, and add that sum to the existing outreach_messages-based sent count so the final "sent" figure for each profile reflects both paths. Also fold in the greatest of dce's last_sent_at into last_outreach_activity so the "last activity" timestamp accounts for drip sends too.

2. Apply the same fix to the identical per-company engagement stats query around line 16373 (the one already fixed to use outreach_messages for the developer-facing outreach analytics page) — add the same drip_campaign_enrollments sum joined through outreach_senders, added to that endpoint's "sent" total.

Keep both fixes purely additive — don't change how outreach_messages is counted, just add the drip campaign total on top of it. Confirm which lines changed in server/routes.ts for each of the two queries.