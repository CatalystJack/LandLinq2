IMPORTANT: actually implement this in code. Do not create a documentation file, notes file, or summary — make the actual code changes described below.

client/src/pages/marketing-home.tsx has a "journey" section (aria-labelledby="platform-journey-title", the one with "See every opportunity clearly," "Underwrite a deal with your assumptions," "Keep the relationship in view," "Make the next conversation useful," "Build follow-up once. Keep it moving," "Know what is moving," and "Ask the workspace what matters next") made up of 6 rows, each rendering an app screenshot mockup component (DashboardMockup, AutoYocMockup, ContactDirectoryMockup, ContactDetailMockup, DripCampaignBuilderMockup, OutreachCampaignsMockup, AIAssistantMockup) inside a `.ll-step-surface` card.

client/src/index.css has real bugs causing both the oversized rows and the inconsistent sizing between them:

1. `.ll-journey-route` (the container that stacks the 6 rows) is defined twice with conflicting gaps: `gap: 7rem` at ~line 2813, then overridden later at ~line 3779 by an unrelated section's rule to `gap: clamp(6rem, 10vw, 10rem)` — which wins because it comes later in the file, inflating the space between every row up to 10rem on wide screens. Rename or scope that later rule (~line 3779, part of a different "editorial" section, not the journey section) so it no longer collides with `.ll-journey-route`, and set the journey section's actual gap to something tighter, like `3.5rem`.

2. `.ll-yoc-step-surface` (the extra class applied only to the Auto YOC row's surface) is only defined in that same unrelated later section (~line 3800: `width: 86%; justify-self: end;`), so it's leaking a width/alignment rule onto just one of the 6 rows that the other 5 don't get — this is a real cause of the rows looking inconsistently sized. Remove the ll-yoc-step-surface class from the Auto YOC row's markup in marketing-home.tsx (~line 739) entirely, so it's styled identically to the other rows via plain `.ll-step-surface`.

3. Standardize the size of all 6 mockup cards instead of letting each mockup component's own internal markup determine its size (which is why they currently render at different sizes — the contact directory/detail/outreach-analytics mockups get a `transform: scale(.82); width: 122%` treatment in one place (~line 2780) and a different `scale(.73); width: 137%` in another (~line 3139-3140), while the AI assistant mockup instead relies on its own `min-height: 390px` (~line 2938), and the others have no scaling applied at all. Replace this per-mockup patchwork with one consistent approach: give `.ll-step-surface` inside this journey section a fixed max-width (e.g. 480px) and fixed height (e.g. 320px) with `overflow: hidden`, and apply a single shared `transform: scale(...); transform-origin: top left;` rule to whatever's inside it (the shared `.ll-app-frame`/`.ll-window` wrapper each mockup already renders into) so every mockup — regardless of its own internal size — gets cropped/scaled down to fit that same fixed box. Pick a scale factor that keeps the screenshots legible at that size.

Keep the existing responsive breakpoints working (the section already has tablet/mobile media queries around ~line 3110-3143 for this same class set) — just make sure the fixed size and scale approach applies consistently across those breakpoints too, proportionally smaller on mobile.

Verify it builds, view the result at a few breakpoints if you can, and confirm via git log that the commit contains a real diff to these files, not a text file.