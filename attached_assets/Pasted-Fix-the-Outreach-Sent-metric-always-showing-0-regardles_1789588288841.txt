Fix the "Outreach Sent" metric always showing 0 regardless of real activity, and two "test email" buttons that fake success without sending anything.

1. In server/routes.ts, the /api/admin/analytics/by-profile endpoint (around line 12846-12995) computes outreach_sent, outreach_opens, outreach_clicks, outreach_replies from a LEFT JOIN LATERAL against outreach_message_events (around line 12916-12929). That table is never written to anywhere in the codebase — it will always return 0. Replace the "sent" count so it comes from the real, populated outreach_messages table instead: COUNT(*) FILTER (WHERE m.status = 'sent') AS sent, joining outreach_messages m to outreach_campaigns c (m.campaign_id = c.id) and filtering c.developer_profile_id = p.id AND COALESCE(c.is_archived, false) = false. Remove the opens/clicks/replies columns from this query's summary and from the SystemWideAnalytics summary object in client/src/pages/executive-dashboard.tsx — we don't have real tracking data for those yet (see item 3), so don't display them as if we do. Keep the KPICard for "Outreach Sent" showing the real sent count.

2. In server/routes.ts, find the identical pattern at the engagement stats query around line 16310-16322 (also selecting FROM outreach_message_events). Apply the same fix: compute "sent" from outreach_messages.status = 'sent' joined through outreach_campaigns filtered by developer_profile_id, and remove opens/clicks/replies from that result since they aren't tracked yet. Update whatever the response object and the frontend page consuming it expect accordingly, without breaking the response shape more than necessary — keep sent as a real field, drop or zero-explain the untracked ones with a short comment.

3. In server/routes.ts around line 22988 (/api/test-email) and around line 23126 (/api/send-test-email), both endpoints build email content but then just log and return { success: true } without ever sending anything (there are comments in both saying essentially "for now, just return success" / "log instead of sending"). Fix both to actually send using the existing, working sendNotificationEmail function from server/emailService.ts (same function server/services/outreachService.ts already uses successfully for real outreach sends). For /api/test-email: call sendNotificationEmail({ to: email, subject: "LandLinq - Branding Settings Test Email", html: testEmailContent, text: "Branding settings test email", type: "test", priority: "high" }) and only return { success: true } if that call returns true; otherwise return a 500 with an error message. For /api/send-test-email: call sendNotificationEmail({ to: testEmail, subject: template.subject, html: template.html, text: template.content, type: "test", priority: "high" }) using the template already fetched a few lines above, and same success/failure handling. Remove the "would send" console.log lines once real sending is wired up.

Do not touch the outreach_messages sending pipeline itself (server/services/outreachService.ts) — it's already correct. Only fix the read-side queries and the two fake test-email endpoints.