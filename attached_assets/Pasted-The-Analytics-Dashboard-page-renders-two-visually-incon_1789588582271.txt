The Analytics Dashboard page renders two visually inconsistent dashboards stacked on top of each other. The top section (KPI cards, Deal Status Distribution donut, Key Performance Metrics) was already redesigned to use Recharts with the shared ChartContainer/ChartTooltip wrapper from @/components/ui/chart and a navy/blue palette. Below it, both client/src/pages/developer-analytics.tsx (line 268) and client/src/pages/analytics-page.tsx (line 672) wrap client/src/components/analytics-dashboard.tsx in a card titled "Advanced Analytics Dashboard" — but that component renders its own internal "Analytics Dashboard" heading (appears at both line 92 and line 190, for the empty and loaded states) and uses Chart.js (react-chartjs-2) instead of Recharts, which is why it looks completely different from everything above it (different fonts, different default colors, cramped card padding, a duplicate title).

Fix client/src/components/analytics-dashboard.tsx:

1. Remove the internal <h3 className="text-lg font-semibold text-catalyst-navy">Analytics Dashboard</h3> heading at both line 92 and line 190 — the parent page already titles this section "Advanced Analytics Dashboard" via its own Card, so this is a redundant duplicate title. Keep the time-range pill selector (7 Days/30 Days/90 Days/1 Year) next to where the heading was, just drop the text label, right-aligning the pills or adding a small "Trend range:" label instead if it looks awkward alone.

2. Migrate all four charts off Chart.js/react-chartjs-2 onto Recharts, matching the pattern already used in client/src/pages/analytics-page.tsx (which imports ChartContainer, ChartTooltip, ChartTooltipContent from @/components/ui/chart, plus Cell, Pie, PieChart as RechartsPieChart, Line, LineChart, Bar, BarChart, XAxis, YAxis from recharts):
   - "Daily Activity Trend" (currently a Chart.js Line at line ~212-241): convert to a Recharts LineChart wrapped in ChartContainer, using the brand navy (#081729) for the line.
   - "Product Type Distribution" (currently a Chart.js Doughnut at line ~244-261): convert to a Recharts donut (Pie with innerRadius, like the Deal Status Distribution donut above it on the page), using a blue-shades palette (#081729, #498EDE, #2563eb, #60a5fa, #93c5fd) instead of the current navy/gold mix.
   - "Pipeline Status" (currently a Chart.js Bar at line ~264-300): convert to a Recharts BarChart. Keep the existing empty-state block (line 286-297) exactly as it is — that one is already correctly styled and was fixed earlier, don't touch it.
   - "Monthly Performance" (currently a Chart.js Bar at line ~303-325): convert to a Recharts BarChart with two series (Submissions in #081729, Closings in #498EDE).
   Remove the chart.js and react-chartjs-2 imports and ChartJS.register(...) call at the top of the file once nothing uses them anymore (check no other file imports from this one relying on chart.js being registered here first).

3. Change every Card in this file from the bare shadcn default to className="rounded-2xl border-slate-200 bg-white shadow-sm" to match the analyticsCardClass treatment used in analytics-page.tsx.

4. In the "Regional Activity Overview" section (line 328-404), change the selected-region highlight from border-catalyst-gold bg-catalyst-gold/10 / hover:border-catalyst-gold/50 to border-catalyst-blue bg-catalyst-blue/10 / hover:border-catalyst-blue/50, so the accent color matches the rest of the app's brand blue instead of gold. Leave the red/orange/yellow/green intensity badges and bars as-is — those colors are meaningful (they encode relative activity level), not just decorative.

After this, both analytics-page.tsx and developer-analytics.tsx should show one continuous, visually consistent dashboard instead of two dashboards in different styles. Don't change the data-fetching logic (the useQuery call, dataOverride/allowFetch props) — only the rendering.