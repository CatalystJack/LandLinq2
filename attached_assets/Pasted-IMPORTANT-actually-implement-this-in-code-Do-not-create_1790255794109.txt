IMPORTANT: actually implement this in code. Do not create a documentation file, notes file, or summary — make the actual code changes described below.

Two changes to the "Product types" editor, in both places it appears: client/src/pages/developer-criteria-settings.tsx (~line 1030-1058) and client/src/pages/admin-investment-companies.tsx (ProductTypeEditorRow, ~line 290-308). Both currently use the same grid layout: `grid gap-4 lg:grid-cols-[minmax(0,1.3fr)_minmax(0,0.7fr)_minmax(0,0.7fr)_minmax(0,0.9fr)_auto] lg:items-end` with columns for Product type, Min acres, Max acres, one rent field, and the remove button.

1. Show both rent fields, not just one. Right now each product type row shows only ONE rent input — "Min rent $/SF" or "Min rent $/Unit" — chosen by the company's rentMetric setting (`isPsf ? "Min rent $/SF" : "Min rent $/Unit"`). Change this so BOTH fields are always visible side by side as two separate NumberFields (minRentPsf and minRentPerUnit), the same way both are already shown in the "State overrides" panel below (which already lists both minRentPsf and minRentPerUnit as override fields — no change needed there). Add a 6th column to the grid-cols template to fit the extra field (adjust the fr values so it still fits reasonably on one row at large widths; wrap to multiple rows on smaller screens the same way the rest of this layout already does). Whichever field corresponds to the company's rentMetric stays the "required" one (red asterisk, validated as it is today) — the other one becomes optional, purely for reference, and is NOT used in deal classification (server/developerClassificationService.ts already only reads whichever metric matches profile.rentMetric — leave that logic exactly as it is; do not make classification start requiring both).

2. Make it clear the top-level product type row is the "Normal" baseline. Right now it's not obvious that the numbers in the main row are the default that applies everywhere except the states listed in "State overrides" below. Add a small label/badge next to the product type name (e.g. "Normal (default)") indicating this. Also update the State overrides panel's description text (currently "Override only the values that differ by state. Blank fields use the flat default above.") to reinforce the same framing, e.g. "States listed below use these numbers instead of the Normal criteria above — leave a field blank to keep the Normal value for that state." Keep the underlying data model and the Add state / override behavior completely unchanged — this is wording/labeling only, not a restructuring.

Verify it builds and confirm via git log that the commit contains a real diff to these two files, not a text file.