Create server/schoolDistrictService.ts. Query NCES's free, official ArcGIS REST endpoint (no API key required): https://nces.ed.gov/opengis/rest/services/School_District_Boundaries/EDGE_SCHOOLDISTRICT_TL23_SY2223/MapServer/0/query, passing the deal's longitude/latitude as geometry (point, geometryType=esriGeometryPoint), spatialRel=esriSpatialRelIntersects, outFields=NAME, f=json. Return the matching district name. Cache the result on the deal record (new schoolDistrict text column) so it's not re-queried on every page load.
Create server/nearbyPermitsService.ts with per-city adapters, since each city publishes permits on its own schema — no fabricated fallback for unsupported cities, return null/"not available" explicitly:
Nashville: Socrata SODA API at https://data.nashville.gov/resource/3h5w-q8b7.json, filtered by a bounding box or $where distance clause around the deal's lat/lng, permits issued in the last 12 months.
Atlanta: ArcGIS REST query against https://dpcd-coaplangis.opendata.arcgis.com/datasets/655f985f43cc40b4bf2ab7bc73d2169b (resolve to its actual FeatureServer/MapServer query endpoint), same radius/date-window approach.
Raleigh: Socrata SODA API at https://data.raleighnc.gov/resource/y5vc-7my7.json (or the "Building Permits Issued Past 180 Days" dataset if better suited), same approach.
Charlotte: leave as a stub returning "not available yet" — I don't have Charlotte's exact dataset resource ID verified; whoever wires this up should grab it from data.charlottenc.gov and follow the same pattern as the other three.
Return a permit count for the trailing 12 months within roughly a 1-mile radius of the deal's coordinates. For any city not in this list, return null so the column shows "Not available for this market" rather than a blank or a guessed number.
Both services should be called from a deal's underlying data-fetch path (wherever deals are enriched with other computed fields like QCT/DDA), so the values are computed once and cached, not recomputed live in the browser on every render.